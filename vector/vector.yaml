api:
  enabled: true
  address: "0.0.0.0:8686"

sources:
  vectorlogs:
    type: internal_logs

  docker:
    type: docker_logs
    exclude_containers:
      - vector

  traefik_access_log:
    type: file
    include:
      - /var/log/traefik/access.log
    read_from: beginning

transforms:
  parse_docker_logs:
    type: remap
    inputs:
      - docker
    source: |
      .container = .container_name ?? .label."com.docker.swarm.service.name" ?? "unknown"
      .image = .image ?? "unknown"

  parse_traefik_access:
    type: remap
    inputs:
      - traefik_access_log
    source: |
      .container = "traefik"
      .log_type = "access"

sinks:
  loki_vectorlogs:
    type: loki
    inputs:
      - vectorlogs
    encoding:
      codec: json
    endpoint: 'http://loki:3100'
    labels:
      logsource: vector

  loki_docker:
    type: loki
    inputs:
      - parse_docker_logs
    encoding:
      codec: json
    endpoint: 'http://loki:3100'
    labels:
      logsource: docker
      container: "{{ container }}"

  loki_traefik_access:
    type: loki
    inputs:
      - parse_traefik_access
    encoding:
      codec: json
    endpoint: 'http://loki:3100'
    labels:
      logsource: traefik
      log_type: access
